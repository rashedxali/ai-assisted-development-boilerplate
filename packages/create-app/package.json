{
  "name": "create-agent-driven-app",
  "version": "0.1.0",
  "description": "Scaffold a new Agent-Driven Development Next.js app with optional features",
  "type": "module",
  "bin": {
    "create-agent-driven-app": "./dist/index.js"
  },
  "files": [
    "dist",
    "template"
  ],
  "scripts": {
    "build": "tsc && node scripts/bundle-template.mjs",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@agent-driven/setup-engine": "workspace:*",
    "@clack/prompts": "^0.10.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
