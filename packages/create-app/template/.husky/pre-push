#!/usr/bin/env sh
set -e

while read local_ref local_sha remote_ref remote_sha; do
  if [ "$remote_ref" = "refs/heads/main" ]; then
    echo "error: direct push to main is not allowed. use a feature branch and open a PR."
    exit 1
  fi
done

npm run build
